name: Velt Engine CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: ${{ matrix.os }} - ${{ matrix.config }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        config: [Release]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwayland-dev libxkbcommon-dev xorg-dev libx11-dev \
            libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev \
            libasound2-dev libpulse-dev libdbus-1-dev

      - name: Setup Vulkan SDK
        uses: humbletim/setup-vulkan-sdk@v1.2.1
        with:
          vulkan-query-version: 1.4.304.1
          vulkan-components: Vulkan-Headers, Vulkan-Loader
          vulkan-use-cache: true

      - name: Install DXC (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          curl -L -o dxc.zip https://github.com/microsoft/DirectXShaderCompiler/releases/download/v1.8.2407/dxc_2024_07_31.zip
          7z x dxc.zip -o${{ github.workspace }}/dxc_pkg
          echo "${{ github.workspace }}/dxc_pkg/bin/x64" >> $GITHUB_PATH

      - name: Install DXC (Linux)
        if: runner.os == 'Linux'
        run: |
          # Fixed URL for Linux
          wget https://github.com/microsoft/DirectXShaderCompiler/releases/download/v1.8.2407/linux_dxc_2024_07_31.tar.gz
          mkdir -p ${{ github.workspace }}/dxc_pkg
          tar -xzf linux_dxc_2024_07_31.tar.gz -C ${{ github.workspace }}/dxc_pkg
          echo "${{ github.workspace }}/dxc_pkg/bin/x64" >> $GITHUB_PATH

      - name: Install macOS Dependencies & DXC
        if: runner.os == 'macOS'
        run: |
          brew install sdl3 vulkan-tools
          # Download DXC for macOS (using the Linux/Universal-ish build or a known mirror)
          # Alternatively, Vulkan SDK often includes glslang; if you MUST have DXC:
          wget https://github.com/microsoft/DirectXShaderCompiler/releases/download/v1.8.2407/linux_dxc_2024_07_31.tar.gz
          mkdir -p ${{ github.workspace }}/dxc_pkg
          tar -xzf linux_dxc_2024_07_31.tar.gz -C ${{ github.workspace }}/dxc_pkg
          echo "${{ github.workspace }}/dxc_pkg/bin/x64" >> $GITHUB_PATH

      - name: Configure CMake
        shell: bash
        run: |
          DXC_BIN=""
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            DXC_BIN="${{ github.workspace }}/dxc_pkg/bin/x64/dxc.exe"
          else
            DXC_BIN="${{ github.workspace }}/dxc_pkg/bin/x64/dxc"
          fi
          
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{ matrix.config }} \
            -DDXC_EXECUTABLE="$DXC_BIN" \
            ${{ runner.os == 'macOS' && '-DAPPLE_VULKAN_SUPPORT=ON' || '' }}

      - name: Build Velt
        run: cmake --build build --config ${{ matrix.config }}
